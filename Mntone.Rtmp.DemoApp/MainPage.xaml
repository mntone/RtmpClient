<Page
    x:Class="Mntone.Rtmp.DemoApp.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Mntone.Rtmp.DemoApp"
	Unloaded="OnPageUnloaded">

	<Grid Background="{StaticResource ApplicationPageBackgroundThemeBrush}">
		<Grid.RowDefinitions>
			<RowDefinition Height="140"/>
			<RowDefinition/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>

		<Grid Grid.Row="1">
			<MediaElement x:Name="mediaElement" AudioDeviceType="Multimedia" RealTimePlayback="True"/>
			<Border x:Name="foregroundElement" Background="Black" Opacity=".4" Visibility="Collapsed"/>
		</Grid>
		
		<StackPanel Grid.Row="2" Orientation="Horizontal">
			<TextBox x:Name="Uri" Text="rtmp://192.168.1.11/flvplayback/test2" Width="300"/>
			<Button Click="OnButtonClicked">Send</Button>
		</StackPanel>
	</Grid>
</Page>
